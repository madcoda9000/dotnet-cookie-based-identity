namespace DotNetIdentity.Models
{
    public enum Gender { Unknown, Male, Female }

    public enum Department { Software, HR, Accounting, Management }

    public enum TwoFactorType { None, Email, Authenticator }
}